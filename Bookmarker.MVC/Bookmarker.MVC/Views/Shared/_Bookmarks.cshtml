@model IEnumerable<Bookmarker.MVC.Models.BookmarkViewModel>

<div>
    @{
        var en = Model.GetEnumerator();
        while(en.MoveNext())
        {
            <div class="row">
                @Html.Partial("_Bookmark", en.Current)
            </div>
        }
     }
</div>
